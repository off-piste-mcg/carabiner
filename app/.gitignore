# Both of these are generated by `xcodegen generate` from project.yml, which is the
# single source of truth — hand edits to either are silently overwritten.
Carabiner.xcodeproj/
Carabiner/Info.plist
build/
.DS_Store
DerivedData/
*.xcuserstate
